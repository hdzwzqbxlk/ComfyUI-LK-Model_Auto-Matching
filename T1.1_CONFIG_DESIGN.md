# T1.1 设计方案 — 统一分散配置（Unify Scattered Config）

> 作者：架构师 高见远（software-architect）｜关联：UPGRADE_PLAN.md T1.1 / PROJECT_BRIEF.md §4 / PHASE0_REPORT.md
> 范围：**仅设计，不写实现代码**。目标为消除「JSON 有一份、代码又硬编码一份」的重复，建立『默认值 < 配置文件 < 环境变量』的单一配置真源，并保证 `/get-config`、`/save-config` 契约与现有行为不变。

---

## 0. 现状盘点（设计依据）

经通读代码，配置目前散落在 **3 个文件 + 多处硬编码**：

| 来源 | 路径 | 内容 | 加载方 | 版本控制 |
|---|---|---|---|---|
| ① 用户密钥 | 根 `config.json` | `{civitai_api_key, huggingface_token}` | `searcher.load_config/save_config`；`__init__.py` 的 `/get-config` 返回并附加 `__version__` | **被 .gitignore 忽略（含密钥）** |
| ② 匹配策略 | `core/data/matcher_config.json` | `{db:{...}, matching:{...}}` | `core/config.py` → `get_matcher_config()` → `matcher.py` / `database.py` | **已提交（committed）** |
| ③ 分词数据字典 | `core/data/models_data.json`（9.8KB） | `{noise_suffixes, protected_terms, model_aliases, popular_models, model_patterns, variant_suffixes, critical_terms}` | `core/utils.py` `load_models_data()`（已外置，仅自带兜底默认值） | 已提交 |
| ④ 硬编码常量 | `database.py` / `utils.py` / `searcher.py` | 评分阈值、超时、缓存 TTL、Provider URL/limit、扫描目录等 | 直接字面量 | — |

### 关键发现（务必注意）
1. **`db.civitai_score` 从未入配置**：`database.py:416,423` 用 `cfg.get('civitai_score', 0.99)`，但 `matcher_config.json` 与 `_DEFAULT_CONFIG` 均无此键 → 实质是**硬编码 0.99**，需补进配置默认值（保持 0.99 不变）。
2. **`db.fuzzy_score_cutoff` 默认字面量误导**：`matcher_config.json` 该键为 `80`，但 `database.py:483` 兜底写 `85`；因键已存在，实际走 80，**85 是死默认值**。清理时以 `80` 为准。
3. **两个 `fuzzy_score_cutoff` 语义不同**：`db.fuzzy_score_cutoff`（SQLite 模糊兜底，80）与 `matching.fuzzy_score_cutoff`（内存模糊匹配，60.0）是**不同阶段**的阈值，**不可合并**，命名需区分清楚。
4. **`searcher` 的 `network.timeout` 配置从未生效**：`searcher.py:27` 读 `self.config.get("network",{}).get("timeout",20)`，但任何配置文件都无 `network` 段 → 永远走 20；而 `HuggingFaceFileSearchProvider.timeout=20`(L314)、`ModelScope=25`(L706)、`CNB=15`(L1213)、`validate=10`(L1307) 又各自硬编码覆盖，行为不一致。
5. **分词大字典已外置**：`NOISE_SUFFIXES` / `COMFYUI_POPULAR_MODELS` 等本就由 `models_data.json` 提供，`utils.py` 仅带兜底默认值 → T1.1 只需把 `models_data.json` **纳入统一加载器**，无需重新外置。
6. **前端契约（已核实 `js/auto_matcher.js`）**：`/get-config` 返回对象被前端读取 `config.version`（:13-14 版本同步）与 `config.civitai_api_key`（:381-382 填入输入框）；`huggingface_token` 虽在响应里但前端暂未直接读取。三者都必须保留。

---

## 1. 目标配置结构

### 方案选择：物理多文件 + 逻辑单加载器（**推荐**）
保留 ① 根 `config.json`（仅密钥，gitignored）与 ② `matcher_config.json`（策略）+ ③ `models_data.json`（分词字典）三个物理文件，但由**扩展后的 `core/config.py` 作为唯一读取入口**把它们合并成一份逻辑配置。

> 理由：（a）`config.json` 含密钥且被 .gitignore，**绝不能并入已提交的单一文件**，否则密钥会被提交或策略无法版本化；（b）`/save-config` 仍只写 `config.json`，`/get-config` 仍只返回其字段 + version，**前端契约零改动**；（c）`models_data.json` 已是外置数据字典，纳入加载器即可。
>
> 备选方案 A（合并为单文件带 section）需一次性迁移脚本把密钥搬进已提交文件、且破坏 gitignore 语义，**不推荐**，仅记录于此。

### 逻辑合并后的完整配置（JSON 示例，含新增段）
```jsonc
{
  // —— 段1：本地匹配（来自 matcher_config.json.db / .matching，已提交）——
  "db": {
    "enabled": true,
    "exact_score": 1.0,
    "basename_score": 0.99,
    "semantic_min_score": 0.35,
    "fuzzy_score_cutoff": 80,
    "civitai_score": 0.99            // 新增：原仅 database.py 硬编码
  },
  "matching": {
    "use_db_first": true,
    "use_exact_match": true,
    "use_fuzzy_match": true,
    "use_variant_match": true,
    "use_legacy_match": true,
    "fuzzy_score_cutoff": 60.0,     // 注意：与 db.fuzzy_score_cutoff 语义不同
    "variant_score_cutoff": 0.9,
    "legacy_score_cutoff": 75
  },

  // —— 段2：网络搜索（新增，来自 matcher_config.json.searcher）——
  "searcher": {
    "network": { "timeout": 20, "impersonate": "chrome124" },
    "cache_ttl": 300,
    "early_stop_score": 0.7,        // 竞速早停阈值（原 searcher.py:1447）
    "high_confidence_score": 0.9,   // 高置信提前返回（原 :449）
    "db_fallback_score": 0.85,      // DB 兜底层阈值（原 :1369）
    "search_term_limit": 5,         // 搜索词上限（原 utils.py:683 [:5]）
    "api": {
      "civitai":    { "url": "https://civitai.com/api/v1/model-versions/by-hash", "limit": 20 },
      "huggingface":{ "limit": 8, "hash_scores": { "exact": 0.98, "dir": 0.95, "file": 0.92 } },
      "google":     { "num": 20 },
      "liblib":     { "limit": 8 }
    },
    "providers": {
      "modelscope": { "timeout": 25, "priority_repos": { "/* 现存 PRIORITY_REPOS 原样迁入 */" } },
      "huggingface": { "scan_dirs": ["/* HF 并发扫描根目录原样迁入 */"] }
      // provider_order 可选覆盖智能路由基础顺序
    }
  },

  // —— 段3：分词数据字典（来自 models_data.json，已提交）——
  "tokenizer": {
    "noise_suffixes": ["fp16", "bf16", "..."],
    "protected_terms": ["sd", "flux", "..."],
    "model_patterns": { "r'v1[\\-_\\.]?5'": "stable diffusion 1.5" },
    "model_aliases": { "sdxl": "stable diffusion xl" },
    "popular_models": { "flux1-dev": "black-forest-labs/FLUX.1-dev" },
    "variant_suffixes": ["q4", "bf16", "..."],
    "critical_terms": ["vae", "lora", "..."],
    // 新增可配置项（原 utils.py 硬编码）：
    "valid_extensions": [".gguf", ".safetensors", ".ckpt", ".pt", ".bin", ".pth", ".onnx", ".pkl"],
    "precision_suffixes": ["-fp8", "-fp16", "-bf16", "_fp8", "_fp16", "_bf16", ".fp8", ".fp16", ".bf16"],
    "noise_words": ["average", "rank", "merged", "combined"],
    "strict_precisions": ["bf16", "fp8", "int8", "int4", "q8"]
  },

  // —— 段4：用户密钥（来自 根 config.json，gitignored）——
  "secrets": {
    "civitai_api_key": "",
    "huggingface_token": ""
  },

  // —— 段5：可演进开关（新增，Phase 2 扩展点）——
  "features": {
    "chinese_tokenization": true,
    "version_aware": false
  }
}
```

> 物理文件边界保持不变：`config.json` 只含 `secrets` 段内容；`matcher_config.json` 含 `db`/`matching`/`searcher`/`features` 段（已提交的策略）；`models_data.json` 含 `tokenizer` 段。加载器在内存中拼成上面的逻辑视图。

---

## 2. 中心配置模块设计（扩展 `core/config.py`）

### 2.1 加载优先级（核心铁律）
```
默认值(_DEFAULT_CONFIG)  <  matcher_config.json(已提交策略)  <  models_data.json(分词字典)
                        <  config.json(用户密钥)             <  环境变量(LK_* 前缀覆盖)
```
- 各文件键空间**互不重叠**（db/matching ↔ searcher/features ↔ tokenizer ↔ secrets），合并用深度递归，不互相覆盖。
- 环境变量仅用于 CI/部署覆盖（如密钥、超时），指向明确键路径。

### 2.2 关键 API 签名
```python
# core/config.py
def load_all_config() -> dict            # 按优先级合并出完整逻辑配置（含 tokenizer 段）
def get_all_config() -> dict             # 带缓存的合并结果（模块级单例）
def get_matcher_config() -> dict         # 返回 {"db":..., "matching":...}（兼容 matcher.py/database.py 既有调用）
def get_searcher_config() -> dict        # 返回 cfg["searcher"]
def get_tokenizer_config() -> dict       # 返回 cfg["tokenizer"]
def get_features() -> dict               # 返回 cfg["features"]（Phase 2 用）
def get_secret(key: str) -> str          # 读 secrets 段（含 env 覆盖），如 "civitai_api_key"
def get_user_config() -> dict            # 返回 secrets 段（供 /get-config 返回）
def save_user_config(data: dict) -> None # 仅写 config.json（供 /save-config）
def override_config(partial: dict) -> dict   # 测试钩子：深度合并进缓存
def reset_config() -> None               # 测试钩子：清空缓存
def validate_config(cfg: dict) -> tuple[bool, list[str]]  # 类型/范围校验（软失败：告警+回退）
```

### 2.3 关键伪代码
```python
import copy, json, os, logging
logger = logging.getLogger("LK.Config")

_DEFAULT_CONFIG = { ... }   # 见 §1 逻辑视图，必须覆盖所有可配置键（含新增 db.civitai_score、searcher.*）

# 环境变量 → 配置路径 白名单（避免任意键注入）
_ENV_MAP = {
    "LK_CIVITAI_API_KEY":      ("secrets", "civitai_api_key"),
    "LK_HUGGINGFACE_TOKEN":    ("secrets", "huggingface_token"),
    "LK_SEARCHER_TIMEOUT":     ("searcher", "network", "timeout"),
    "LK_SEARCHER_EARLY_STOP":  ("searcher", "early_stop_score"),
    "LK_SEARCHER_CACHE_TTL":   ("searcher", "cache_ttl"),
    "LK_SEARCH_TERM_LIMIT":    ("searcher", "search_term_limit"),
}
_ROOT = os.path.dirname(os.path.dirname(__file__))
_DATA = os.path.join(os.path.dirname(__file__), "data")
_CONFIG_PATH_USER   = os.path.join(_ROOT, "config.json")          # 密钥（gitignored）
_CONFIG_PATH_STRATEGY = os.path.join(_DATA, "matcher_config.json")
_CONFIG_PATH_TOKENIZER = os.path.join(_DATA, "models_data.json")

def _deep_merge(base, override):
    merged = copy.deepcopy(base)
    for k, v in (override or {}).items():
        if isinstance(v, dict) and isinstance(merged.get(k), dict):
            merged[k] = _deep_merge(merged[k], v)
        else:
            merged[k] = copy.deepcopy(v)
    return merged

def _load_json(path):
    try:
        with open(path, "r", encoding="utf-8") as f:
            return json.load(f)
    except FileNotFoundError:
        return {}
    except Exception as e:                       # 结构错误：软失败，回退默认并告警
        logger.warning("[Config] 解析失败 %s: %s，使用默认值", path, e)
        return {}

def _apply_env(cfg):
    for env_key, path in _ENV_MAP.items():
        val = os.environ.get(env_key)
        if val is None:
            continue
        node = cfg
        for part in path[:-1]:
            node = node.setdefault(part, {})
        node[path[-1]] = _coerce(val, _default_at(path))   # 按默认类型强制转换

def load_all_config():
    cfg = copy.deepcopy(_DEFAULT_CONFIG)
    cfg = _deep_merge(cfg, _load_json(_CONFIG_PATH_STRATEGY))   # 策略
    tokenizer = _load_json(_CONFIG_PATH_TOKENIZER)              # 分词字典
    cfg["tokenizer"] = _deep_merge(cfg.get("tokenizer", {}), tokenizer) if tokenizer else cfg.get("tokenizer", {})
    cfg["secrets"] = _deep_merge(cfg.get("secrets", {}), _load_json(_CONFIG_PATH_USER))  # 密钥
    _apply_env(cfg)
    ok, errs = validate_config(cfg)
    for e in errs:
        logger.warning("[Config] %s", e)        # 值错误：告警+已在 validate 中回退默认
    return cfg

# 既有 get_matcher_config 保持返回 {"db","matching"}，调用点零改动
```

### 2.4 校验策略（软失败，而非硬崩溃）
ComfyUI 节点**必须在加载期不抛异常**，故 `validate_config` 采用：
- **结构错误**（JSON 语法错、文件缺失）→ 回退默认 + 告警（不崩溃）。
- **值错误**（score 不在 [0,1]、cutoff 非数字、use_* 非布尔）→ 把该键回退到 `_DEFAULT_CONFIG` 同路径值 + 告警。
- 不向外抛 `ConfigError`（与 UPGRADE_PLAN「fail-fast」的取舍：节点加载安全优先；CI 可加 `--strict-config` 开关在测试环境启用硬失败）。

---

## 3. 去重清单（逐项：文件:行 → 现值 → 目标 key）

> 优先级：P0=消除「JSON+代码双份」或补缺失键；P1=行为相关、应入配置且 env 可覆盖；P2=清理字面量/可选外置。

### 3.1 `core/database.py`（P0 重点）
| 位置 | 现值 | 目标 key | 说明 |
|---|---|---|---|
| :389 | `cfg.get('exact_score', 1.0)` | `db.exact_score` | 去掉字面量兜底，直接读合并后配置 |
| :399 | `cfg.get('basename_score', 0.99)` | `db.basename_score` | 同上 |
| :416, :423 | `cfg.get('civitai_score', 0.99)` | `db.civitai_score`（**新增**） | 该键原不存在，需补进 `_DEFAULT_CONFIG` 并保持 0.99 |
| :467 | `cfg.get('semantic_min_score', 0.35)` | `db.semantic_min_score` | 去字面量 |
| :483 | `cfg.get('fuzzy_score_cutoff', 85)` | `db.fuzzy_score_cutoff` | 删除误导的 85 兜底（实际走 80） |

### 3.2 `core/matcher.py`（已配置驱动，仅清理字面量）
| 位置 | 现值 | 目标 key | 说明 |
|---|---|---|---|
| :161 | `db_cfg.get('semantic_min_score', 0.35)` | `db.semantic_min_score` | 合并后已保证存在，可去掉兜底 |
| :432 | `self.config['matching'].get('fuzzy_score_cutoff', 60.0)` | `matching.fuzzy_score_cutoff` | 清理字面量（P2） |
| :486 | `variant_score_cutoff` | `matching.variant_score_cutoff` | 同上（P2） |
| :512 | `legacy_score_cutoff` | `matching.legacy_score_cutoff` | 同上（P2） |

### 3.3 `core/utils.py`
| 位置 | 现值 | 目标 key | 说明 |
|---|---|---|---|
| :683 | `unique_terms[:5]` | `searcher.search_term_limit` | **P1**，影响搜索词数量 |
| :349 | `valid_extensions = {...}` | `tokenizer.valid_extensions`（**新增**） | P2 |
| :790 | `precision_suffixes = [...]` | `tokenizer.precision_suffixes`（**新增**） | P2 |
| :492 | `noise_words = {...}` | `tokenizer.noise_words`（**新增**） | P2 |
| :895 | `STRICT_PRECISIONS = {...}` | `tokenizer.strict_precisions`（**新增**） | P2 |
| :33-97, :101-198, :201-213 | `NOISE_SUFFIXES`/`PROTECTED_TERMS`/`MODEL_PATTERNS`/`MODEL_ALIASES`/`COMFYUI_POPULAR_MODELS`/`VARIANT_SUFFIXES`/`CRITICAL_TERMS` | `tokenizer.*` | **已自 `models_data.json` 读取**，无需重新外置，仅改为经 `get_tokenizer_config()` 取数 |

### 3.4 `core/searcher.py`（P1 重点：网络/评分）
| 位置 | 现值 | 目标 key | 说明 |
|---|---|---|---|
| :25, :315, :707 | `"chrome124"` | `searcher.network.impersonate` | P1 |
| :27 | `.get("network",{}).get("timeout",20)` | `searcher.network.timeout` | P1（原 `network` 段不存在，永远 20） |
| :314 | `self.timeout = 20` (HF) | `searcher.network.timeout` | P1 |
| :706 | `self.timeout = 25` (ModelScope) | `searcher.providers.modelscope.timeout` | P1 |
| :1213 | `timeout=15` (CNB) | `searcher.network.timeout` 或 `providers.cnb.timeout` | P1 |
| :1307 | `timeout=10` (validate) | `searcher.network.timeout` | P2 |
| :309, :692 | `CACHE_TTL = 300` | `searcher.cache_ttl` | P1 |
| :449 | `score >= 0.9` | `searcher.high_confidence_score` | P1 |
| :1447 | `score >= 0.7` | `searcher.early_stop_score` | P1（竞速早停，核心阈值） |
| :1369 | `score >= 0.85` | `searcher.db_fallback_score` | P1 |
| :194, :261 | `limit=20` | `searcher.api.civitai.limit` / `.huggingface.limit` | P2 |
| :394 | `limit=8` (Liblib) | `searcher.api.liblib.limit` | P2 |
| :933 | `num=20` (Google) | `searcher.api.google.num` | P2 |
| :588/:594/:601 | `0.98/0.95/0.92` | `searcher.api.huggingface.hash_scores` | P2 |
| :235 | `0.7 + 0.3` 权重 | `searcher.api.civitai.score_weights` | P2 |
| :695 | `PRIORITY_REPOS` | `searcher.providers.modelscope.priority_repos` | P2（数据字典迁入） |
| ~:504 | HF 并发扫描根目录列表 | `searcher.providers.huggingface.scan_dirs` | P2（数据） |
| :1273-1282 | Provider 基础顺序 | `searcher.provider_order`（可选覆盖） | P2 |

---

## 4. 向后兼容与迁移

1. **`config.json` 形状不变**：仍为 `{civitai_api_key, huggingface_token}`；`searcher.save_config` 改为委托 `config.save_user_config` 仅写此文件 → `/save-config` 契约不变。
2. **`/get-config` 响应不变**：`searcher.get_config()` 改为返回 `config.get_user_config()`（即 secrets 段）；`__init__.py` 仍附加 `__version__`。响应仍含 `civitai_api_key`/`huggingface_token`/`version`，前端零改动。
3. **`matcher_config.json` 扩展而非破坏**：新增 `searcher`/`features` 段与 `db.civitai_score` 键；旧部署若无这些键，加载器用 `_DEFAULT_CONFIG` 自动补齐 → **无需一次性迁移脚本**。建议随 PR 提交一份更新后的 `matcher_config.json`（值=默认值）作为文档化范本。
4. **`models_data.json` 不变**：仅改用统一加载器读取，文件内容不动。
5. **环境变量覆盖**：CI/部署用 `LK_CIVITAI_API_KEY` 等覆盖，无需触碰磁盘文件。
6. **回归保护**：`regression_tests/matcher_config_regression.py` 用 `override_matcher_config({...})` 改 `db`/`matching` —— 保留 `override_matcher_config`（或统一为 `override_config`），其行为（深合并进 `_DEFAULT_CONFIG`）维持不变。

---

## 5. 前端契约影响（明确声明）

| 改动 | 是否影响 `/get-config` 响应 | 是否影响前端读取 |
|---|---|---|
| 新增 `searcher`/`features` 段（仅后端逻辑配置） | 否（不写入 `/get-config` 响应） | 否 |
| `config.json` 仍只返回密钥+version | 否，形状不变 | 否 |
| `db.civitai_score` / `searcher.*` 新增键 | 否（属后端配置，不进响应） | 否 |
| 若未来需要把 feature flag 透传给前端 | 可在 `/get-config` 响应追加顶层键（前端只读已知键，忽略未知键，**安全**） | 需前端确认读取新键才生效 |

**铁律**：`civitai_api_key`、`huggingface_token`、`version` 三个键**禁止改名/删除**；`/get-config` 响应为「向后兼容的开放对象」，新增键安全。

---

## 6. 给工程师的实现任务清单（有序、含依赖）

- **T1.1.1 扩展 `core/config.py` 为中心加载器**（P0｜无依赖｜基础）
  - 落地 §2 的 `_DEFAULT_CONFIG`（含 `db.civitai_score`、`searcher.*`、`features`）、`_ENV_MAP`、`load_all_config`/`get_all_config`/`get_matcher_config`(兼容既有返回)/`get_searcher_config`/`get_tokenizer_config`/`get_secret`/`get_user_config`/`save_user_config`/`override_config`/`reset_config`/`validate_config`。
  - 建立 §4 的加载优先级与软失败校验。

- **T1.1.2 更新 `core/data/matcher_config.json`**（P0｜依赖 T1.1.1 的默认 schema｜可与 T1.1.1 并行起草）
  - 增补 `db.civitai_score:0.99`、`searcher` 段（network/cache_ttl/early_stop_score/high_confidence_score/db_fallback_score/search_term_limit/api/providers）、`features` 段。值=默认值，行为不变。

- **T1.1.3 折叠 `models_data.json` 进统一加载器**（P1｜依赖 T1.1.1）
  - `utils.py` 的 `load_models_data()` 改为从 `config.get_tokenizer_config()` 取数；`models_data.json` 文件保持，新增 `valid_extensions`/`precision_suffixes`/`noise_words`/`strict_precisions` 四个键（值=当前硬编码）。

- **T1.1.4 替换 `database.py` 硬编码**（P0｜依赖 T1.1.1、T1.1.2）
  - 按 §3.1 去除 `exact_score`/`basename_score`/`semantic_min_score` 字面量兜底；`civitai_score` 改读 `db.civitai_score`；`fuzzy_score_cutoff` 去掉 85 误导兜底（走 80）。行为不变验证。

- **T1.1.5 替换 `utils.py` 硬编码**（P1｜依赖 T1.1.1、T1.1.3）
  - `search_term_limit`(P1)、`valid_extensions`/`precision_suffixes`/`noise_words`/`strict_precisions`(P2) 改为从 `get_tokenizer_config()` 读取；`[:5]`、`STRICT_PRECISIONS` 等字面量移除。

- **T1.1.6 替换 `searcher.py` 硬编码 + 接入统一密钥**（P1｜依赖 T1.1.1）
  - `network.timeout`/`impersonate`、`cache_ttl`、`early_stop_score`、`high_confidence_score`、`db_fallback_score`、`api.*.limit`、`search_term_limit` 改读 `get_searcher_config()`；
  - `load_config`/`get_config`/`save_config` 委托 `config.get_user_config()`/`save_user_config()`（保持 `/save-config`、`/get-config` 契约）。

- **T1.1.7 兼容与迁移验证**（P0｜依赖 T1.1.1–T1.1.6）
  - 旧 `matcher_config.json`（无新段）仍可加载（默认补齐）；`config.json` 密钥保留；环境变量 `LK_*` 覆盖生效；`/get-config` 形状回归。

- **T1.1.8 测试与验收**（P0｜依赖全部）
  - 新增：`tests/test_config.py` 覆盖加载优先级、env 覆盖、校验回退、向后兼容（`/get-config` 形状、旧配置补齐）；
  - 运行 `regression_tests/matcher_config_regression.py` + 既有匹配回归，确认行为零变化。

---

## 7. 风险与注意点

1. **两个 `fuzzy_score_cutoff` 语义不同**（db 段 80 vs matching 段 60.0）：命名/注释必须区分，切忌合并或误用。
2. **`civitai_score` 新增键**：默认必须设为 `0.99`（=原硬编码），否则 DB 路径 Civitai 别名匹配评分会变。
3. **`db.fuzzy_score_cutoff` 兜底字面量 85→80**：因 JSON 已有 80，删 85 无行为变化；但若有人手删 JSON 该键，将回退到默认 80（非 85），属预期。
4. **节点加载不可崩溃**：校验采用软失败（告警+回退），不以 `ConfigError` 硬抛；如需 CI 硬失败可加 `--strict-config` 开关。
5. **密钥与策略分离**：`config.json` 被 .gitignore，切勿并入已提交单文件；统一加载器只在内存合并。
6. **环境变量命名**：统一 `LK_` 前缀，避免与 ComfyUI/系统环境变量冲突。
7. **分词字典已外置**：`models_data.json` 已承载大字典，T1.1 只统一加载入口，**勿重复维护两份**。
8. **避免过度外置**：Provider 内部数据（`PRIORITY_REPOS`、扫描目录、URL）量大，本阶段仅接入 timeout/limit/评分等「影响行为与可 env 覆盖」的项；纯数据字典可留代码或后续外置，控制 T1.1 范围。
9. **缓存与测试**：模块级单例需 `reset_config()` 清缓存；既有 `reset_matcher_config` 保留或统一。
10. **行为不变性对照（实现时逐条核对）**：新默认值必须等于当前有效值——`search_term_limit=5`、`early_stop_score=0.7`、`high_confidence_score=0.9`、`db_fallback_score=0.85`、`cache_ttl=300`、`network.timeout=20`、`impersonate=chrome124`、`civitai_score=0.99`。
11. **线程安全**：ComfyUI 进程内模块仅加载一次，单例缓存无并发写风险；`save_user_config` 为低频写盘，无需加锁（保持现状）。
```
